---
import { t, type Locale } from '../i18n/ui';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
---

<section id="contact" class="relative overflow-hidden">
  <div class="absolute inset-0 -z-10 bg-gradient-to-br from-sky-100 via-cyan-50 to-white"></div>
  <div class="mx-auto max-w-6xl px-6 py-10 md:py-14">
    <h2 class="text-3xl font-semibold text-slate-900 md:text-4xl">{t(locale, 'contact.title')}</h2>
    <p class="mt-3 max-w-2xl text-base text-slate-600 md:text-lg">{t(locale, 'contact.subtitle')}</p>
    <div class="mt-8 grid gap-6 md:grid-cols-4">
      <div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm md:col-span-1">
        <ul class="space-y-4 text-base text-slate-700">
          <li>
            <span class="block text-sm uppercase tracking-wide text-slate-500">{t(locale, 'contact.email')}</span>
            <a href="mailto:info@saiora.ai" class="mt-1 inline-block font-medium text-amber-500 transition hover:text-amber-600">info@saiora.ai</a>
          </li>
          <li>
            <span class="block text-sm uppercase tracking-wide text-slate-500">{t(locale, 'contact.phoneTelegram')}</span>
            <a href="tel:+998974078822" class="mt-1 inline-block font-medium text-amber-500 transition hover:text-amber-600">+998 97 407 88 22</a>
          </li>
        </ul>
      </div>

      <form
        name="contact"
        method="POST"
        netlify
        data-netlify="true"
        netlify-honeypot="bot-field"
        class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm md:col-span-3"
      >
        <input type="hidden" name="form-name" value="contact" />
        <p class="hidden">
          <label>
            Don’t fill this out:
            <input name="bot-field" />
          </label>
        </p>
        <input type="hidden" name="locale" value={locale} />

        <label class="mb-4 block text-base">
          <span class="mb-2 block text-slate-700">{t(locale, 'contact.form.contact')}</span>
          <input
            name="contact"
            type="text"
            required
            placeholder={t(locale, 'contact.form.contactPlaceholder')}
            class="w-full rounded-xl border border-slate-300 bg-white px-4 py-3 text-slate-900 outline-none transition focus:border-sky-500"
          />
        </label>

        <label class="mb-4 block text-base">
          <span class="mb-2 block text-slate-700">{t(locale, 'contact.form.task')}</span>
          <textarea
            name="task"
            rows="5"
            required
            class="w-full rounded-xl border border-slate-300 bg-white px-4 py-3 text-slate-900 outline-none transition focus:border-sky-500"
          ></textarea>
        </label>

        <button
          type="submit"
          class="rounded-xl border border-[#8cc3f1] bg-[#8cc3f1] px-6 py-3 text-base font-semibold text-slate-900 transition hover:bg-[#7db8ea]"
        >
          {t(locale, 'contact.form.button')}
        </button>
      </form>
    </div>
  </div>
</section>
