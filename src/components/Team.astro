---
import { t, type Locale } from '../i18n/ui';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;

const members = [
  { name: 'team.aybek.name', role: 'team.aybek.role', description: 'team.aybek.description' },
  { name: 'team.luiza.name', role: 'team.luiza.role', description: 'team.luiza.description' },
  { name: 'team.victor.name', role: 'team.victor.role', description: 'team.victor.description' }
];
---

<section id="team" class="relative overflow-hidden">
  <div class="absolute inset-0 -z-10 bg-gradient-to-br from-sky-100 via-cyan-50 to-white"></div>
  <div class="mx-auto max-w-6xl px-6 py-10 md:py-14">
    <h2 class="text-3xl font-semibold text-slate-900 md:text-4xl">{t(locale, 'team.title')}</h2>
    <div class="mt-8 grid gap-6 md:grid-cols-3">
      {
        members.map((member) => (
          <article class="rounded-2xl border border-slate-200 bg-white p-6 text-center shadow-sm">
            <h3 class="text-xl font-semibold text-slate-900">{t(locale, member.name)}</h3>
            <p class="mt-1 text-base font-medium text-sky-700">{t(locale, member.role)}</p>
            <p class="mt-3 whitespace-pre-line text-base leading-relaxed text-slate-600">{t(locale, member.description)}</p>
          </article>
        ))
      }
    </div>
  </div>
</section>
